name: Build and deploy React client to Azure Web App - react104

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js version
        uses: actions/setup-node@v3
        with:
          node-version: '22.x'

      - name: Install and build React client
        working-directory: ./client
        run: |
          npm install
          npm install --save-dev @babel/plugin-proposal-private-property-in-object
          CI=false npm run build

      - name: Upload artifact for deployment
        uses: actions/upload-artifact@v4
        with:
          name: react-client
          path: ./client/build

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: react-client
          path: ./build

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_E864894B75704DB5A318F8F95A0B71F9 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_C5F7C4A3B23C43D587FA25CECDA5D290 }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_353A21132D8A42579F7A46D533A61EAF }}

      - name: Deploy React client to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'react104'
          slot-name: 'Production'
          package: ./build
